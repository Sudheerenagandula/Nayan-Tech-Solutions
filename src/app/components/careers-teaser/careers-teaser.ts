import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-careers-teaser',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './careers-teaser.html',
  styleUrl: './careers-teaser.css'
})
export class CareersTeaser {}
